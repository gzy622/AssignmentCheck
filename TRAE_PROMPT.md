# Trae AI 任务提示词 - 模块化重构

---

## 智能体指令

请执行 `MODULE_REFACTOR_CHECKLIST.md` 中标记的下一项待完成任务。

### 任务来源

检查清单文件：`c:\Users\VG622\Documents\AssignmentCheck\MODULE_REFACTOR_CHECKLIST.md`

### 执行规则

1. **按顺序执行**：从检查清单中当前标记为 `[ ]` 的第一项开始，完成后再勾选 `[x]`
2. **原子化原则**：每次只完成一个最小任务单元，不要跨任务跳跃
3. **验证后再推进**：每个任务完成后必须验证功能正常，再继续下一项
4. **保持兼容性**：任何时候不能破坏现有功能，特别是数据导出必须始终可用
5. **渐进式提交**：每完成 2-3 个小任务就提交一次，提交信息清晰说明变更内容

### 工作流程

1. 打开 `MODULE_REFACTOR_CHECKLIST.md` 找到下一个待完成任务
2. 阅读任务描述，理解目标和验收标准
3. 在 `index.html` 中找到相关代码
4. 创建或修改对应模块文件（`src/modules/` 目录下）
5. 保持原有功能不变，只进行代码结构重构
6. 在浏览器中验证功能正常
7. 更新检查清单进度
8. 提交代码变更

### 重要约束

- **禁止删除旧代码**：过渡期用新模块封装旧逻辑，保留原有函数作为桥接
- **禁止修改数据格式**：localStorage 中的数据格式必须保持兼容
- **禁止引入外部依赖**：只使用原生 JavaScript，保持轻量化
- **必须添加版本号**：每次修改项目后同时更新代码版本号

### 代码规范

- 使用 IIFE 模式实现模块化
- 模块依赖通过参数注入（参考检查清单中的标准模板）
- 复杂逻辑必须添加注释说明
- 函数命名使用驼峰式，清晰表达用途

### 当前进度

请先告诉我：
1. 检查清单中下一项待完成的任务是什么？
2. 该任务的具体要求是什么？

然后我会等待你的具体指示，或者你可以直接说"开始执行下一项任务"，我将自动从检查清单中提取并执行。
